1️⃣ What is the difference between var, let, and const?
Ans : var is function-scoped and outdated. let is block-scoped . const is block-scoped and cannot be reassigned .var can be access before declaring and it will show undefined so it is outdated.


2️⃣ What is the spread operator (...)?
Ans : It expands an array or object into individual elements.

3️⃣ What is the difference between map(), filter(), and forEach()?
Ans : Map() transforms each element and returns a new array. filter() keeps only matching elements also returns. forEach() just loops it doesnt returns .

4️⃣ What is an arrow function?
Ans : A shorter way to write functions using =>.

5️⃣ What are template literals?
Ans : Strings that are written with backticks .it use ${} to embed other things in string.